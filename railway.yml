version: '1.0'
services:
  web:
    env:
      PORT: 8080
    github:
      branch: master
    buildCommand: pip install -r requirement.txt
    runCommand: uvicorn main:app --host 0.0.0.0 --port $PORT

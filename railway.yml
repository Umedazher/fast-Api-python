version: '1.0'
services:
  web:
    env:
      PORT: 8080
    github:
      branch: main
    buildCommand: pip install -r requirements.txt
    runCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
